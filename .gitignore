node_modules/
dist/
data/
# The test suite has its own database and artifacts folder; both are rebuilt
# by the next run and neither belongs in the repository.
artifacts-test/
.env
.env.local
*.log
.tmp/

# Worktrees created by Claude Code sessions. Each is a full second checkout
# of this repository; none of it belongs in the repository itself.
.claude/worktrees/
